# Generated by Isengard v0.3c compose-import
# Do not edit if you want regeneration to overwrite you.
# Move to Isengard-managed mode with: isd adopt <stack>  (v0.3d)
services:
  hello:
    image: nginxdemos/hello:plain-text
    container_name: hello-web
    ports:
    - 0.0.0.0:8080:80
    environment:
      TZ: UTC
    healthcheck:
      test:
      - CMD
      - wget
      - -qO-
      - http://localhost/
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 5s
    restart: unless-stopped
    labels:
      isengard.enable: 'true'
      isengard.expose.host: hello.local
    networks:
    - hello_default
networks:
  hello_default:
    external: true
